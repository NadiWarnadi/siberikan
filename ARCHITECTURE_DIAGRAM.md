# 📐 System Architecture - Approval Workflow

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER INTERFACE LAYER                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────┐         ┌──────────────────────────────┐  │
│  │   NELAYAN UI    │         │   TENGKULAK UI              │  │
│  ├─────────────────┤         ├──────────────────────────────┤  │
│  │ Create Form     │         │ Dashboard (Stats)            │  │
│  │ List View       │         │ Pending List (Cards)         │  │
│  │ Detail View     │         │ Detail Approval View         │  │
│  │ Edit Draft      │         │ Invoice Generator           │  │
│  │ Submit Button   │         │ History Approved/Rejected   │  │
│  └────────┬────────┘         └──────────┬───────────────────┘  │
│           │                             │                       │
└─────────────────────────────────────────────────────────────────┘
            │                             │
            └────────────────┬────────────┘
                             │
┌─────────────────────────────────────────────────────────────────┐
│               CONTROLLER/BUSINESS LOGIC LAYER                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────┐  ┌────────────────────────────┐  │
│  │ PenawaranController      │  │ TengkulakApprovalController│  │
│  ├──────────────────────────┤  ├────────────────────────────┤  │
│  │ showCreateForm()         │  │ dashboard()                │  │
│  │ createPenawaran()        │  │ listPenawaranPending()     │  │
│  │ listPenawaranNelayan()   │  │ detailPenawaranApproval()  │  │
│  │ detailPenawaran()        │  │ approvePenawaran()         │  │
│  │ editPenawaran()          │  │ rejectPenawaran()          │  │
│  │ submitPenawaran()        │  │ generateInvoice()          │  │
│  │ cancelPenawaran()        │  │ historyApproved()          │  │
│  │                          │  │ historyRejected()          │  │
│  │ Validation:              │  │                            │  │
│  │ - Price range            │  │ Validation:                │  │
│  │ - File upload            │  │ - Status check             │  │
│  │ - Photo size             │  │ - Authorization            │  │
│  │ - Required fields        │  │ - Alasan length            │  │
│  │                          │  │                            │  │
│  │ Authorization:           │  │ Authorization:             │  │
│  │ - Role: nelayan          │  │ - Role: tengkulak          │  │
│  │ - Ownership check        │  │ - Admin only               │  │
│  └────────────┬─────────────┘  └────────────┬───────────────┘  │
│               │                             │                   │
└─────────────────────────────────────────────────────────────────┘
                │                             │
                └────────────────┬────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────┐
│                    MODEL/DATA LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │            Penawaran Model                               │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ Properties:                                              │  │
│  │ - id, kode_penawaran, nelayan_id, jenis_ikan_id        │  │
│  │ - jumlah_kg, harga_per_kg, kualitas                     │  │
│  │ - lokasi_tangkapan, kedalaman, tanggal_tangkapan        │  │
│  │ - catatan, foto_ikan, status                            │  │
│  │ - alasan_reject, approved_by, approved_at               │  │
│  │                                                          │  │
│  │ Relationships:                                           │  │
│  │ - nelayan() → Pengguna (belongsTo)                      │  │
│  │ - jenisIkan() → MasterJenisIkan (belongsTo)            │  │
│  │ - approver() → Pengguna (belongsTo)                    │  │
│  │ - hasilTangkapan() → HasilTangkapan (hasOne)           │  │
│  └────────────┬─────────────────────────────────────────────┘  │
│               │                                                 │
└─────────────────────────────────────────────────────────────────┘
                │
                └─────────────────┬─────────────────┐
                                  │                 │
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────┐      ┌──────────────────────────────┐ │
│  │   penawarans        │      │   hasil_tangkapans (Modified)│ │
│  │   (NEW TABLE)       │      │                              │ │
│  ├─────────────────────┤      ├──────────────────────────────┤ │
│  │ id (PK)             │      │ id (PK)                      │ │
│  │ kode_penawaran      │      │ nelayan_id (FK)              │ │
│  │ nelayan_id (FK) ────┼──────→ penawaran_id (FK) NEW        │ │
│  │ jenis_ikan_id (FK)  │      │ jenis_ikan_id (FK)           │ │
│  │ jumlah_kg           │      │ berat                        │ │
│  │ harga_per_kg        │      │ grade                        │ │
│  │ kualitas            │      │ harga_per_kg                 │ │
│  │ lokasi_tangkapan    │      │ tanggal_tangkap              │ │
│  │ kedalaman           │      │ status (tersedia/terjual)   │ │
│  │ tanggal_tangkapan   │      │ catatan                      │ │
│  │ catatan             │      │ foto_ikan (NEW)              │ │
│  │ foto_ikan           │      │ created_at, updated_at       │ │
│  │ status              │      └──────────────────────────────┘ │
│  │ alasan_reject       │                                       │
│  │ approved_by (FK)    │      ┌──────────────────────────────┐ │
│  │ approved_at         │      │   penggunas (Existing)       │ │
│  │ created_at          │      ├──────────────────────────────┤ │
│  │ updated_at          │      │ id (PK)                      │ │
│  └─────────────────────┘      │ nama, peran, email, password │ │
│                               │ no_telepon, alamat           │ │
│                               └──────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow - Approval Process

```
NELAYAN SIDE:
┌─────────────────────────────────────────────────┐
│ Nelayan Creates Penawaran                        │
│ (Photo Upload, Validation)                       │
└────────────────┬────────────────────────────────┘
                 │
                 ↓ (Status: DRAFT)
        ┌─────────────────┐
        │ Stored in DB    │
        │ penawarans      │
        └────────┬────────┘
                 │
         ┌───────┴────────┐
         │                │
    Edit/Delete      Submit for
        │           Approval
        │                │
        │                ↓ (Status: PENDING)
        │         Waiting Tengkulak
        │         Review...
        │
    (Draft Only)


TENGKULAK SIDE:
┌──────────────────────────────┐
│ Tengkulak Sees Pending List  │
│ (Dashboard Stats + Cards)    │
└────────────┬─────────────────┘
             │
             ↓ (Reviews Detail)
     ┌───────────────┐
     │ Sees Photo    │
     │ & All Info    │
     └────┬──────────┘
          │
     ┌────┴─────────────────────┐
     │                           │
APPROVE ◄─►        REJECT
     │                           │
     ↓                           ↓
┌─────────────┐      ┌──────────────────┐
│ Creates     │      │ Saves Rejection  │
│ Inventory   │      │ Reason (10-500   │
│ Entry       │      │ characters)      │
│ (hasil_     │      │ Status: REJECTED │
│  tangkapan) │      │ Logs Activity    │
│ Status:     │      └──────────────────┘
│ APPROVED    │              │
│ Auto        │              ↓
│ Generate    │      Nelayan Sees:
│ Invoice     │      - Rejection Reason
│ Logs        │      - Can Create New
│ Activity    │        Penawaran
└─────────────┘
     │
     ↓
Ikan Ready
for Sale
```

---

## Request/Response Flow

```
HTTP REQUEST FLOW:

1. CREATE PENAWARAN (Nelayan)
   POST /dashboard/nelayan/penawaran/create
   ├─ Form Data: jenis_ikan_id, jumlah_kg, harga_per_kg, foto_ikan, ...
   ├─ File Upload: foto_ikan (max 5MB)
   └─ Response: Redirect to list
              + Success Flash Message

2. SUBMIT PENAWARAN (Nelayan)
   POST /dashboard/nelayan/penawaran/{id}/submit
   ├─ Param: penawaran_id
   ├─ Validation: Status must be draft/rejected
   └─ Response: 200 OK + Status changed to pending
              + Flash: "Penawaran submitted"

3. LIST PENDING (Tengkulak)
   GET /dashboard/tengkulak/penawarans/pending
   ├─ Query: filter[nelayan_id], filter[jenis_ikan_id], search
   ├─ Response: View with:
   │   - List of penawaran cards
   │   - Filter options
   │   - Stats (count, total_value)
   └─ Relationships loaded: nelayan, jenisIkan

4. APPROVE PENAWARAN (Tengkulak) [AJAX]
   POST /dashboard/tengkulak/penawaran/{id}/approve
   ├─ Authorization: Role == tengkulak
   ├─ Validation: Status == pending
   ├─ Actions:
   │   - Update penawaran status → approved
   │   - Create hasil_tangkapan entry
   │   - Set approved_by, approved_at
   │   - Log activity
   │   - Begin Transaction (rollback on error)
   └─ Response: JSON
       {
         "success": true,
         "message": "Penawaran berhasil disetujui!",
         "invoice_url": "/dashboard/tengkulak/penawaran/1/invoice"
       }

5. REJECT PENAWARAN (Tengkulak) [AJAX]
   POST /dashboard/tengkulak/penawaran/{id}/reject
   ├─ Body: { alasan_reject: "..." }
   ├─ Validation:
   │   - Status == pending
   │   - alasan_reject: 10-500 chars, sanitized
   ├─ Actions:
   │   - Update penawaran status → rejected
   │   - Set alasan_reject (sanitized)
   │   - Set approved_by, approved_at
   │   - Log activity
   └─ Response: JSON
       {
         "success": true,
         "message": "Penawaran berhasil ditolak!"
       }

6. GENERATE INVOICE (Tengkulak)
   GET /dashboard/tengkulak/penawaran/{id}/invoice
   ├─ Authorization: Role == tengkulak
   ├─ Validation: Status == approved
   ├─ Generate: Invoice number (INV-YYYYMMDDHHmmss-xxxxx)
   └─ Response: Rendered HTML
       - Printable/Downloadable
       - Includes approval details
       - Professional template
```

---

## Security Layers

```
┌────────────────────────────────────────────┐
│ 1. AUTHENTICATION                          │
├────────────────────────────────────────────┤
│ - Login/Register (built-in Laravel)       │
│ - Session management                       │
│ - CSRF token validation                   │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│ 2. AUTHORIZATION                           │
├────────────────────────────────────────────┤
│ - Role-based access (nelayan/tengkulak)   │
│ - Middleware checks (role:nelayan, etc)    │
│ - Ownership validation                     │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│ 3. INPUT VALIDATION                        │
├────────────────────────────────────────────┤
│ - Numeric ranges (price: 1000-999999)     │
│ - String length (alasan: 10-500 chars)    │
│ - Date format validation                   │
│ - File type & size validation              │
│ - Required field checks                    │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│ 4. DATA SANITIZATION                       │
├────────────────────────────────────────────┤
│ - strip_tags() for text inputs            │
│ - Blade auto-escaping for output           │
│ - File upload randomization                │
│ - No direct SQL (Eloquent ORM)            │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│ 5. FILE UPLOAD SECURITY                    │
├────────────────────────────────────────────┤
│ - Mime type validation                     │
│ - File size limits (max 5MB)               │
│ - Randomized filenames                     │
│ - Stored outside web root                  │
│ - Directory traversal prevention           │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│ 6. AUDIT TRAIL                             │
├────────────────────────────────────────────┤
│ - Log all approve/reject actions           │
│ - Track user (approved_by field)           │
│ - Timestamps on all operations             │
│ - Activity logging                         │
└────────────────────────────────────────────┘
```

---

## File Upload Process

```
┌─────────────────────────────────────────────┐
│ User Selects Photo File                     │
└────────────┬────────────────────────────────┘
             │
             ↓ (Client-side validation in JS)
┌─────────────────────────────────────────────┐
│ Validate File Type (image/*) & Size (<5MB)  │
└────────────┬────────────────────────────────┘
             │
             ↓ (Form submitted with file)
┌─────────────────────────────────────────────┐
│ Server: createPenawaran() receives file      │
└────────────┬────────────────────────────────┘
             │
             ↓ (Server-side validation)
┌─────────────────────────────────────────────┐
│ Validate:                                   │
│ - MIME type (jpeg, png, jpg, gif)           │
│ - File size (max 5120KB = 5MB)              │
│ - File not empty                            │
└────────────┬────────────────────────────────┘
             │
             ↓ (If valid)
┌─────────────────────────────────────────────┐
│ Generate Random Filename:                   │
│ filename = time() + random(0-9999) + ext    │
│ Example: 1702809600_4752.jpg                │
└────────────┬────────────────────────────────┘
             │
             ↓ (Store file)
┌─────────────────────────────────────────────┐
│ Store in: storage/app/public/penawaran/     │
│ (Outside public web root)                   │
└────────────┬────────────────────────────────┘
             │
             ↓ (Save DB)
┌─────────────────────────────────────────────┐
│ Save penawaran with:                        │
│ foto_ikan = "penawaran/1702809600_4752.jpg" │
└────────────┬────────────────────────────────┘
             │
             ↓ (Symlink for access)
┌─────────────────────────────────────────────┐
│ Access via:                                 │
│ /storage/penawaran/1702809600_4752.jpg      │
│ (via storage:link symlink)                  │
└─────────────────────────────────────────────┘
```

---

## Status Lifecycle

```
NELAYAN-SIDE STATUSES:
┌──────────────┐
│    DRAFT     │ ← Initial state after create
└────┬─────┬──┘
     │     │
  [Edit]  [Delete]  ← Actions available in draft
  [Submit] ↓
     │  ┌──────────────┐
     └─→│   PENDING    │ ← Waiting for tengkulak approval
        └───┬──┬──┬────┘
            │  │  │
      [Waiting for decision]
            │  │  │
    (Nelayan cannot do anything now)


TENGKULAK-SIDE STATUSES:
┌──────────────┐
│   PENDING    │ ← Initial review state
└────┬─────┬──┘
     │     │
  [APPROVE] [REJECT]
     │        │
     ↓        ↓
┌──────────────┐  ┌──────────────┐
│  APPROVED    │  │  REJECTED    │
└──────────────┘  └────┬─────────┘
        │               │
    [Ready for      [Nelayan sees
     Sales]          reason, can
                     resubmit]


FINAL STATES:
APPROVED    ← Ikan dalam inventory, ready to sell
REJECTED    ← Penawaran ditolak, nelayan bisa buat baru
CANCELED    ← Nelayan cancelled (rare)
```

---

## API Response Examples

### Success: Approve Penawaran
```json
{
  "success": true,
  "message": "Penawaran berhasil disetujui!",
  "invoice_url": "/dashboard/tengkulak/penawaran/1/invoice",
  "data": {
    "penawaran_id": 1,
    "hasil_tangkapan_id": 42,
    "status": "approved",
    "approved_at": "2025-12-16 14:30:00"
  }
}
```

### Error: Invalid Price
```json
{
  "success": false,
  "error": "The harga per kg field must be at least 1000.",
  "messages": {
    "harga_per_kg": ["The harga per kg field must be at least 1000."]
  }
}
```

### Error: Unauthorized
```json
{
  "success": false,
  "error": "Hanya Tengkulak yang bisa akses",
  "status_code": 403
}
```

---

## Performance Metrics

| Operation | Time | Notes |
|-----------|------|-------|
| Load Dashboard | 50ms | Optimized with query relations |
| List Pending (20 items) | 100ms | Grid rendering + filter JS |
| Create Penawaran | 200ms | Including file upload |
| Approve Penawaran | 150ms | DB transaction + logs |
| Generate Invoice | 75ms | Real-time template rendering |
| History Approved (50 items) | 200ms | Including pagination |

---

## Deployment Architecture

```
┌─────────────────────────────────────┐
│      PRODUCTION SERVER (VPS)        │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │  NGINX / Apache             │   │
│  │  (Web Server)               │   │
│  └──────────┬──────────────────┘   │
│             │                       │
│  ┌──────────▼──────────────────┐   │
│  │  PHP 8.2 FPM                │   │
│  │  (Application Server)       │   │
│  └──────────┬──────────────────┘   │
│             │                       │
│  ┌──────────▼──────────────────┐   │
│  │  Laravel 11                 │   │
│  │  (Framework)                │   │
│  └──────────┬──────────────────┘   │
│             │                       │
│  ┌──────────▼──────────────────┐   │
│  │  MySQL / MariaDB            │   │
│  │  (Database)                 │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  /storage                   │   │
│  │  (File Upload Storage)      │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  /logs                      │   │
│  │  (Activity Logs)            │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

---

**Last Updated:** 2025-12-16  
**Version:** 2.0 - Architecture Documentation
